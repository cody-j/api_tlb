The Local Bodega - API

# Run API

`$ python3 -m venv ./.venv`

`$ . ./.venv/bin/activate`

`$ pip install -r ./api/requirements.txt`

`$ pip install -r ./orm/requirements.txt`

`$ uvicorn api.api:app --reload`

